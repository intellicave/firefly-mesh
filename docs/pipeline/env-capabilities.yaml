# AutoDev Pipeline — Environment Capabilities (edge run)
# Probed: 2026-05-09 by autodev step 1.4  (supersedes 2026-04-29 classic run)
# Platform: win32 / Windows 11 Home China 10.0.26200
# Project: firefly-mesh edge (v1.0)

probed_at: "2026-05-09T00:00:00Z"
platform: "win32"

tools:
  node:
    available: true
    version: "v24.13.0"
    evidence: "node --version exit 0"

  pnpm:
    available: true
    version: "10.30.3"
    evidence: "pnpm --version exit 0"

  git:
    available: true
    version: "2.52.0.windows.1"
    in_git_repo: true
    user: "Leooo-Huang"
    evidence: "git rev-parse --is-inside-work-tree = true"

  wrangler:
    available: false
    global: false
    via_npx: false
    evidence: "wrangler not in PATH; not in root package.json deps yet"
    impact: "Cannot deploy to Cloudflare Workers until wrangler added to services/hub devDeps"
    blocks:
      - "M0: Cloudflare Workers actual deploy (code scaffold unblocked)"
    workaround: "Add wrangler to services/hub/package.json devDeps; use pnpm exec wrangler"

  gh_cli:
    available: true
    version: "2.87.0"
    evidence: "gh --version exit 0"

  docker:
    available: true
    version: "29.2.1"
    evidence: "docker --version exit 0"

  vercel_cli:
    available: false
    evidence: "not needed — edge uses Cloudflare Pages, not Vercel"

# === Edge project docs status ===
edge_docs_state:
  ideation_md: "done"    # 2026-05-08-firefly-mesh-edge-ideation.md
  design_md: "done"      # 2026-05-08-firefly-mesh-edge-design.md
  meta_md: "done"        # 2026-05-08-firefly-mesh-edge-meta.md
  plan_md: "done"        # 2026-05-08-firefly-mesh-edge-plan.md
  index_md: "done"       # 2026-05-08-firefly-mesh-edge-index.md
  rules_md: "done"       # 2026-05-08-firefly-mesh-edge-rules.md
  ui_md: "missing"       # Phase 3 TODO
  api_md: "missing"      # Phase 4 TODO

# === Sleep-mode policy ===
sleep_mode_policy:
  rule: "Per CLAUDE.md, any 'cannot do X because Y missing' claim must
         reference a verified available=false row above."
